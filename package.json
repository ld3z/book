{
  "name": "book",
  "private": true,
  "scripts": {
    "icons": "node scripts/generate-icons.js",
    "serve": "node scripts/generate-icons.js && zola serve"
  },
  "devDependencies": {
    "@iconify/json": "^2.2.435"
  }
}